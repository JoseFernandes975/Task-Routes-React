import './styles.css';

export default function NotFound(){
  return(
    <main>
        <section id="jf-section-not-found" className="jf-container">
            <h1>Não Encontrado!</h1>
        </section>
    </main>
  )
}